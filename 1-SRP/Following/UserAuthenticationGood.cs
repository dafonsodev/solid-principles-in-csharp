namespace _1_SRP.Following
{
    public static class UserAuthenticationGood
    {
        public static bool Authenticate(string email, string password)
        {
            // Code to authenticate user against a database
            return true;
        }
    }
}
